apiVersion: v1
kind: Service
metadata:
  name: xiaozhi-retrieval-runtime
  labels:
    app.kubernetes.io/name: xiaozhi-retrieval-runtime
    app.kubernetes.io/part-of: xiaozhi
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: xiaozhi-retrieval-runtime
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP
